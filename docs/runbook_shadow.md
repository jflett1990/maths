# Shadow Mode Runbook
- Set `mode: shadow`.
- Keep `mode_controls.shadow_observe_only: true`.
- System observes/scans but does not place orders.
- Verify report has `markets_rejected_by_reason.shadow_observe_only`.
